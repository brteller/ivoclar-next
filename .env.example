# Required for page data API (build-page-speed)
API_KEY_SECRET=your_api_key_here

# Optional: form submissions POST to this URL (e.g. webhook or backend)
# If unset, /api/contact still returns 200 and form redirects to thank-you
# CONTACT_WEBHOOK_URL=https://your-backend.com/contact
